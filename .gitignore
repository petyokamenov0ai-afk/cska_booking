# dependencies
node_modules/
.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# testing
/coverage/
/playwright-report/
/test-results/
/blob-report/
/.playwright/

# env
.env
.env*.local
!.env.example

# pipeline intermediates (huge, regenerable). The emitted artifacts
# (data/stadium.json, data/stadium-overview.svg, data/stadium.sample.json) ARE
# committed — deploys seed from them and cannot run the Python pipeline.
/.venv-pipeline/
/scripts/pipeline/build/
/scripts/pipeline/renders/
/scripts/pipeline/crops/
/scripts/pipeline/out/
/scripts/pipeline/__pycache__/
*.pyc

# os / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# local-only compose overrides (host port conflicts differ per machine)
docker-compose.override.yml
